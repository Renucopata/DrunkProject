
describe('LoginPage', () => {



    it('should pass too', () => {
        // Arrange
        const a = 5;
        const b = 7;

        // Act
        const result = a + b;

        // Assert
        expect(result).toBe(12);
    });
    it('should pass as well', () => {
        // Arrange
        const a = 5;
        const b = 7;

        // Act
        const result = a + b;

        // Assert
        expect(result).toBe(12);
    });
    it('should pass maybe', () => {
        // Arrange
        const a = 5;
        const b = 7;

        // Act
        const result = a + b;

        // Assert
        expect(result).toBe(12);
    });
    it('should pass perhaps', () => {
        // Arrange
        const a = 5;
        const b = 7;

        // Act
        const result = a + b;

        // Assert
        expect(result).toBe(12);
    });
    it('should def pass', () => {
        // Arrange
        const a = 5;
        const b = 7;

        // Act
        const result = a + b;

        // Assert
        expect(result).toBe(12);
    });

});
